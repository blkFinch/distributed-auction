package Bank;

/**
 * Task runner using a concurrent linked list to prevent the concorrent
 * bank threads from executing simultaneously. This is the only thread that should
 * call SQL.
 */
public class TaskRunner {
}
